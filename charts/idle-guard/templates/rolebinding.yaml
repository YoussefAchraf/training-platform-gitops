apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: idle-guard
  labels:
    app.kubernetes.io/name: idle-guard
    app.kubernetes.io/part-of: training-platform
subjects:
  - kind: ServiceAccount
    name: idle-guard
roleRef:
  kind: Role
  name: idle-guard
  apiGroup: rbac.authorization.k8s.io
