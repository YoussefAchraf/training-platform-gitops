apiVersion: v2
name: chatbot
description: >-
  n8n + its dedicated redis + metrics-exporter, deployed and rolled back
  together as one logical unit — see ARCHITECTURE-PLAN.md §2/§6.
type: application
version: 0.1.0
appVersion: "1.0.0"
