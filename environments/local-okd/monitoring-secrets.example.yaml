# NOT applied by Helm/ArgoCD — copy-paste template. Real Secret created
# out-of-band; *-secrets.yaml is gitignored, never committed.
#
#   oc create secret generic grafana-admin-credentials \
#     --namespace monitoring \
#     --from-literal=admin-user='admin' \
#     --from-literal=admin-password='<a real strong password>'
#
# Keys expected by charts/monitoring (kube-prometheus-stack grafana.admin):
# admin-user, admin-password
