# NOT applied by Helm/ArgoCD — this is a copy-paste template. Create the
# real Secret out-of-band, once, by hand (same "apply by hand" pattern as
# bootstrap/), and this chart references it via `existingSecret`. Never
# commit the filled-in version — *-secrets.yaml is gitignored.
#
#   oc create secret generic postgres-credentials \
#     --namespace training-platform \
#     --from-literal=postgres-password='<a real strong password>'
#
# Key expected by charts/postgres: postgres-password
