# Same apps-crc.testing subdomain convention as the other three routes
# (frontend/backend/n8n) - see docs/runbook.md for the matching
# /etc/hosts entries this needs on the host machine. Split out of
# monitoring-values.yaml alongside charts/monitoring-routes - see that
# chart's Chart.yaml for why.
route:
  host: "grafana-training-platform.apps-crc.testing"
  prometheusHost: "prometheus-training-platform.apps-crc.testing"
