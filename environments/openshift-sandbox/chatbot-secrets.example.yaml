oc create secret generic chatbot-credentials \
  --namespace achrafyoussef-ay-dev \
  --from-literal=N8N_ENCRYPTION_KEY='<generate: openssl rand -hex 16>' \
  --from-literal=N8N_OWNER_PASSWORD='<a real strong password, editor login>' \
  --from-literal=AI_API_KEY='<your AI provider key>' \
  --from-literal=REDIS_PASSWORD='<pick a redis password — this is the CHATBOT redis, distinct from backend-redis>'
