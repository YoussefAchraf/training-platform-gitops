{
  "name": "training-platform-gitops-scripts",
  "private": true,
  "version": "1.0.0",
  "description": "Standalone operational scripts for this gitops repo (vault-seed.js) — no runtime dependencies by design.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "vault-seed": "node vault-seed.js"
  }
}
